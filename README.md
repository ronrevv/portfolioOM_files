# OM W MOTIONS — Cinematographer Portfolio

Live portfolio website for **OM Vaisnav (OM W MOTIONS)** — Cinematographer crafting Commercial, Narrative, Documentary, and Music Video films.

---

## 🚀 Instant Local Preview

The site is currently running locally at:
👉 **[http://localhost:3000](http://localhost:3000)**

To run it anytime from terminal:
```bash
# Option 1: Using npm (recommended)
npm start

# Option 2: Using Python
python3 -m http.server 3000
```

---

## 🌐 Deploy Live to the Internet (Free)

### 1. Vercel (Fastest — 1 Click)
```bash
npx vercel
```

### 2. Netlify
```bash
npx netlify deploy --prod --dir=.
```

### 3. GitHub Pages
1. Push this repository to GitHub.
2. Go to **Settings** > **Pages** > Select `main` branch > `/ (root)` > Save.
3. Your site will be live at `https://<username>.github.io/<repo-name>/`.

### 4. Cloudflare Pages
1. Connect your GitHub repository to Cloudflare Pages.
2. Build command: None (Static site).
3. Output directory: `/`.

---

## ✨ Features & Architecture

- **Cinema Aesthetic**: Premium dark mode by default with tailored warm amber accents (`#c8965a`) and smooth Light Mode toggle.
- **Hero Canvas**: Ambient floating golden dust particles reacting dynamically to ambient motion.
- **Custom Cursor**: Dual-element precision dot and lagging follower ring with magnetic hover states.
- **Scroll Progress & Header Blur**: Real-time scroll indicator and glassmorphic navigation header.
- **Interactive Lightbox Modal**: Click any project card to view technical camera specs, lenses, color grading notes, and direct links to 4K Master Reels on Google Drive.
- **Dynamic Gallery**: Categorized filtering across *Wedding*, *Fashion*, *Documentary*, *Cultural*, and *Commercial*.
- **Client Testimonials**: Interactive tab switcher with quotes from directors, brides, and producers.
- **Direct Connect**:
  - **WhatsApp Direct**: Pre-fills project inquiry messages to `+91 9660845727`.
  - **Email**: Direct link to `omwmotions@gmail.com`.
  - **Instagram**: Link to `@omwmotion`.
  - **Google Drive**: Full 4K portfolio master collection.
